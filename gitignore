# ── Andesine — .gitignore ─────────────────────────────────────────────────

# Dependencies (installed via npm install)
node_modules/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# ⚠ DO commit package-lock.json — it's required for `npm ci` in Docker
